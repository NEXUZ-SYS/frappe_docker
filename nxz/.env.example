# --- imagem custom ---
CUSTOM_IMAGE=nxz-erpnext
CUSTOM_TAG=v16
PULL_POLICY=never

# --- porta e site ---
HTTP_PUBLISH_PORT=8080
SITES=erpnext.localhost
FRAPPE_SITE_NAME_HEADER=erpnext.localhost

# --- credenciais (TROCAR antes de qualquer exposição fora de localhost) ---
DB_PASSWORD=nxz_mariadb_dev_password
ADMIN_PASSWORD=nxz_admin_dev_password

# --- versões (uso informativo; build-args ficam no build.sh) ---
ERPNEXT_VERSION=version-16
